language: python

env:
    - NLTK_DATA=$HOME/nltk_data

python:
    - "2.7"
    # - "3.4"  # gevent is failing for 3.4 todo identify all dependencies

install:
    - sudo apt-get install libblas-dev liblapack-dev gfortran  # needed for scipy
    - pip install -r bot-requirements.txt -r app-requirements.txt
    - python ./scripts/download_nltk_data.py

script:
    - make test

after_success:
    - coveralls
